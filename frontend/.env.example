# Frontend environment variables for Vite
# Rename this file to .env (do NOT commit real secrets) and set the values.
# WARNING: Anything prefixed with VITE_ is embedded in the client bundle and visible to users.
# Use a secure backend proxy for truly private keys.

VITE_GEMINI_API_KEY=YOUR_GEMINI_KEY_HERE
